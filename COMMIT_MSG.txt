feat: add trade management risk fields and manual keyword sync

- 保留策略手册关键词手动输入方案：左侧策略列表、右侧关键词复习和核心分析同步显示，同一关键词固定同一颜色
- Add targetPrice, exitReason, managementTag, managementReview and targetReached fields
- Calculate realizedR from actual P&L divided by initial risk dollars
- Add dashboard/core analysis metrics for realized R, payoff ratio and missed target profit
- Let trade strategy use existing strategy names through a datalist while still allowing custom input
- Sync edited trade form values into trade table and K-line review panels immediately
- Update K-line trade number markers so losing trades are red
- Update README and CSV example for the new import/export fields

Note: after deployment, existing cloud rows continue to work because new fields live inside the JSON data payload.
