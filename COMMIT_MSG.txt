feat: 新增K线图复盘、响应式布局，破规则改为手动标记

- K线图页：导入当日 ATAS K线CSV，按进出场时间/点位自动标注交易，
  进出场按顺序编号 ①②③，下方按顺序列出当日每笔交易
  （策略 / 入场信号 / 入场点位选择原因 / 备注）
- 交易录入新增字段：入场信号(entrySignal)、入场点位选择原因(entryReason)，
  并接入 CSV 导入/导出（含中文表头）
- 破规则(ruleBroken)改为纯人工标记，移除按执行评分和关键词的自动推断，
  破规则理由统一写在备注
- 响应式布局：移除固定宽度，新增 900px/560px 断点，移动端侧边栏收顶、网格堆叠
- 数据表数字列右对齐 + 等宽对齐(tabular-nums)
- 修复 Avg Loss 显示为红色，盈亏配色一致

不影响 Supabase 云端同步、空开始机制与现有存储结构
