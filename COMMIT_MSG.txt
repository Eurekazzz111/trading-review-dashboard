feat: organize K-line review by traded days

- Only show chart dates that have both trade records and imported K-line data
- Hide the chart, order path and trade detail panel when no matching trade day exists
- Add year/month navigation with a compact monthly trade-day grid
- Keep imported non-trade dates stored but hidden until a trade is recorded for that date
- Migrate local K-line persistence from localStorage to IndexedDB for longer 1m history
- Keep the existing Supabase kline_days schema and cloud sync format compatible
- Update README with the new display and storage behavior
